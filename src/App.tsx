import "./App.scss";
import { Flow } from "./lib/components";

const App = () => {
  return (
    <main className="app">
      <section className="app__section">
        <Flow />
      </section>
    </main>
  );
};

export default App;
